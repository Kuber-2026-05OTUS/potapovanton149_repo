apiVersion: v2
name: web-server
description: A Helm chart for Kubernetes web server
type: application
version: 0.1.0
appVersion: "1.16.0"

dependencies:
  - name: redis
    version: "20.0.0"
    repository: "file://charts/redis"
    condition: redis.enabled
    tags:
      - cache
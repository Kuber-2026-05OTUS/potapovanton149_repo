apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "web-server.fullname" . }}-cm
  labels:
    {{- include "web-server.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configmap.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
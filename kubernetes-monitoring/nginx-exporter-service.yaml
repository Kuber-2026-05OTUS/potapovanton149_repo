apiVersion: v1
kind: Service
metadata:
  name: nginx-exporter
  namespace: homework
  labels:
    app: nginx-exporter
spec:
  selector:
    app: nginx-exporter
  ports:
  - port: 9113
    targetPort: 9113
    name: metrics
  type: ClusterIP
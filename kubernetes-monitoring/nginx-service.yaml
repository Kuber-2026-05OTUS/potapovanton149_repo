apiVersion: v1
kind: Service
metadata:
  name: nginx-with-metrics
  namespace: homework
  labels:
    app: nginx-with-metrics
spec:
  selector:
    app: nginx-with-metrics
  ports:
  - port: 80
    targetPort: 80
    name: http
  - port: 8080
    targetPort: 8080
    name: metrics
  type: ClusterIP